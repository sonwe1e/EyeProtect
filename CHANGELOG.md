# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[Semantic Versioning](https://semver.org/lang/zh-CN/)。本机运行数据不会随版本升级迁移
到云端；安装版使用稳定的用户数据目录，portable 继续使用 EXE 同目录数据，见 README。

## [1.6.0] - 2026-09-20

### Added
- 主进程统一日志入口 `src/main/logger.ts`（`debug/info/warn/error`，`EYEPROTECT_VERBOSE=1` 开启 debug）。
- 渲染器 `useConfirm + ConfirmDialog`：替代三处 `window.confirm`（任务完成确认、删除确认、专注替换确认），走 `Dialog` 焦点陷阱。
- 休息提示音：新增 `soundEnabled` / `soundVolume`（默认开启、音量 0.6）。开始与结束播放 Web Audio 合成的轻柔铃声，不打包音频文件；遮罩右上角可一键静音，设置页可试听。
- 全屏免打扰：新增 `fullscreenDndEnabled`（默认关闭）。检测到全屏应用时把这次提醒推迟 5 分钟，不需要手工维护白名单；原有会议应用白名单的判定保持不变。
- 桌宠右键菜单：立即休息（护眼 / 走动 / 合并）、暂停与恢复提醒、开始或停止专注、切换桌宠外观与小动作、打开工作台各页、召回桌宠到当前屏幕、隐藏桌宠。
- 遮罩提醒支持键盘操作：`Space` / `Enter` 开始或完成休息，`Esc` 稍后，`S` 跳过，按钮上直接标出对应快捷键。
- 托盘菜单新增“召回桌宠到当前屏幕”。

### Changed
- 主进程 `pomodoro/runtimeState/reminderSurface/diagnostics/index` 的直接 `console.*` 收敛到 `logger`，发布日志只有一级前缀。
- `tests/quality-step.test.ts` 锁定“无 `window.confirm` + ConfirmDialog 已接入 + logger 可用”，并改为断言桌宠的专注入口来自右键菜单。
- `scripts/smoke-simple-experience.mjs` 重新对齐当前界面：休息卡改用“艺术舞台 + 阅读面板”的 `.rest-badge` / `.rest-stage-count` / `.rest-activity` 标记，主按钮按禁用态倒计时文案断言，桌宠专注入口改为右键菜单可达性检查。打包验收由此恢复全绿。
- 确认弹窗收尾：`title/confirmText/danger` 可配，删除默认聚焦取消键并用 danger 样式。
- 任务行：行内控件不再冒泡展开详情，`•••` 支持 `Esc` 关闭，带 `reminderAt` 的行显示提醒标记，搜索无匹配给空态。
- 任务编辑：外部 revision 变化回同步 draft，未改时保存禁用并换 `CommandButton` 反馈。
- 气泡按钮最小 36px，关闭键 32px。
- 休息文案统一：Alert 与 gentle 气泡共用 `restLede`；主按钮禁用时显示剩余秒数。
- 稍后提醒：遮罩上的“稍后”只影响当次，不再内联改全局默认；默认稍后时长移入设置页。
- 桌宠小动作（眨眼/伸懒腰）可在设置关闭（`petMotion`）；系统“减少动态效果”始终优先。
- 提醒遮罩与工作台改为落在鼠标所在的那块显示器上；取不到光标时回退到桌宠所在屏幕，再回退主显示器，兼容多显示器、虚拟显示器与远程桌面。
- 设置页把提醒方式（沉浸遮罩 / 浮窗卡片 / 轻柔气泡）、提示音与免打扰开关一并集中在“休息提醒”一组，保持设置页只有三组。
- `logger.error/warn` 通过注入 sink 写入 `reminder-trace.log`（`log-warn/log-error` 事件），打包版有一条完整落盘排障链。

### Removed
- 桌宠窗口的常驻工具栏（待办页签、专注按钮、设置齿轮）与 `pet-toolbar-compact` 样式：这些入口合并进右键菜单。

### Fixed
- `logger` 不再把调用方的参数数组直接交给 sink 处理。
- 遮罩窗口弹出时主动取得焦点，键盘快捷键在提醒出现后立即可用。

## [1.4.0] - 2026-09-11

### Added
- 精简工作台：只保留待办、完成记录、设置三个视图；任务按截止日期分组，原地展开备注、日期、单次提醒和一层步骤，支持清单筛选与搜索。
- 休息提醒卡片：遮罩窗口改为“艺术舞台 + 阅读面板”，显示提醒类型、主进程选中的微休息建议动作及步骤进度、倒计时环，以及开始休息/完成/稍后/跳过。
- 主进程番茄钟：计时、10 秒检查点、锁屏与休眠暂停、重启一律恢复为暂停，并与手选待办共用同一个气泡浮窗。

### Changed
- 任务模型简化为“截止日期 + 一层步骤”；旧多层级子任务、时间盒、独立提醒、规划与专注运行逻辑停用，数据仍随备份保留、可只读查看和恢复。
- 休息提醒统一由遮罩窗口承担；开始休息才暂停专注，休息结束后手动继续，短休息到点不自动完成任务。
- 桌宠只订阅待办计数与提醒状态，不再接收全量任务列表。
- 桌宠外观只保留内置的橘猫、小狗、白兔三只像素动物。

### Removed
- 组合/收藏角色系统：程序化角色生成、角色收藏与命名、材质与配饰、每日访客，以及养成的分数、心情与配饰状态一并删除。
- 旧的提醒卡与提醒气泡样式、旧设置窗口与旧验收脚本随之清理。

### Fixed
- 设置与完成记录页内容超出窗口时无法滚动：工作台改为自带滚动容器，不再受 `body { overflow: hidden }` 影响。
- 像素动物图形尺寸改为显式声明，不再依赖隐式 SVG 尺寸推导。

## [1.3.0] - 2026-08-30

### Added
- 项目“分组”升级为工作流视角：汇总进行中/完成任务、今日与累计投入时间、最近进展和下一步。
- 任务检查点：暂停、切换或手动记录时保存“做到哪里、回来先做什么、此刻感受”。
- 一键专注与任务切换：任务列表/看板直接进入计时；切换时可记录检查点并原子结束旧会话、开始新会话。
- 今日复盘收录未计划但实际投入过的任务、任务切换检查点、自由反思和明日第一步。
- 备份格式 v6，完整包含任务检查点与每日反思，并保持旧版本备份导入兼容。

### Changed
- 暂停专注后保留当前任务和恢复上下文，不再要求重新寻找任务。
- 安装版数据迁移到 `%APPDATA%\\eye-protect-pet\\data`；portable 与开发模式保持原有本地目录语义。
- NSIS 升级会在旧应用退出后、旧卸载器运行前安全迁移旧 `$INSTDIR\\data`，目标已存在时不覆盖。
- 原生 DPI 验收使用主机真实 `devicePixelRatio`，覆盖桌宠、日程和看板拖拽。

### Fixed
- 命令面板首焦点落在关闭按钮、全局快捷键穿透模态层的问题。
- HTML5 日程拖拽的 `copy`/`move` 语义冲突，真实 pointer 拖拽现在可以落入时间线。
- 分数 DPI 下桌宠首次拖拽会同时发生 1–2 像素隐式缩放的问题。

## [1.2.0] - 2026-08-16

### Added
- 统一工作台：今天 / 收件箱 / 日程 / 专注 / 项目 五个主视图，今日复盘、独立提醒、公仔收藏、设置四个工具视图。
- v1.1 任务核心（SQLite Task Core）：任务/项目/子任务/标签/重复规则/上下文（桌面·外出·任意）/休息时提醒/预估时长。
- 每日计划（DailyTaskPlan）与每日容量；TimeBlock 日程块（一个任务可拆多个块）；项目看板分组（Section）。
- 专注会话（FocusSession）：单一全局会话、健康休息暂停不中断、四种时间层（本次/今日/累计/计划）。
- 每日复盘（Daily Review）：计划 vs 实际、Today's 3 完成、专注与健康休息汇总。
- 独立提醒：一次性/每天/工作日/每周/每 N 天，与护眼提醒共用调度内核与持久化投递队列。
- 本地健康周报与护理反馈；自适应节奏建议（样本不足时保持基准）。
- 崩溃恢复：进行中的休息会话与调度快照持久化，重启后恢复呈现。
- 桌面暗化遮罩（focused 模式）改为半透明暗化（原为全黑）。
- 主进程未捕获异常/未处理拒绝写入滚动诊断日志（reminder-trace.log）。

### Fixed
- 任务并发编辑保护（baseRevision 乐观锁）在 IPC 路径上生效。
- 独立提醒送达失败后不再被后续 re-arm 无限重放。
- 设置变更后新的 pre-alert 导前时间立即应用于当前周期。
- 深色主题下徽章/芯片对比度（约 2.1:1 → 使用语义对比令牌）。
- 快速连按箭头键/切换材质不再被 in-flight 去重静默丢弃。
- 空标题编辑回退并提示，而非静默保留旧值。
- 崩溃恢复的提醒在启动时立即呈现。
- 通知 click 终态优先于 close 事件。

### Changed
- `tests/` 纳入 `npm run typecheck` 严格类型检查（修复 64 处潜在类型错误）。
- 仓库行尾统一为 LF（新增 `.gitattributes`）。
- 移除遗留的 TodoItem 显示排序死代码与旧 panel/alarm/todo 界面。

## [1.0.0] — 2026-05

- 常驻托盘与透明桌宠；护眼/走动提醒间隔、合并展示、完成/稍后/跳过/暂停。
- 活跃使用时间计时的提醒周期；系统空闲/锁屏/休眠冻结与自然离开重置。
- 提醒 surface 降级链（主窗口 → 应急窗口 → 系统通知 → 托盘状态）。
- 程序化内联 SVG 公仔：每日随机访客、收藏、改名、材质、配饰、固定出场。
- 设置：跟随系统/浅色/深色主题、舒适/紧凑密度、免打扰时段、前台应用白名单、全局快捷键。
- 完整 JSON 备份与导入回滚快照；数据库升级确认与恢复模式。
- Windows x64 NSIS 安装包与 portable exe。

## [0.5.0] — 2026-04

- 修复 sandboxed portable 运行时数据目录解析；加固提醒运行时与窗口生命周期。

## [0.4.0] — 2026-03

- 待办体验重做；提醒屏上合并与稍后计数；待办气泡与闹钟面板窗口。

## [0.3.0] — 2026-02

- 强制休息模式（锁定提醒动作直到计时结束）；提醒/闹钟/待办独立窗口与持久化。

> 更早的 0.1–0.2 迭代为原型阶段，未进入本记录。
